{% extends "partials/base.njk" %}

{% block title %}Tienda en línea de Pacer{% endblock %}

{% set meta_description = "Catálogo de productos | Pasarelas de pagos | Contacto y ubicación | Diseño Web y móvil | Link a Redes Sociales | Búsqueda integrada | Blog integrado" %}
{% set og_url = "https://getmore.mx/es/portafolio/pacer.html" %}
{% set og_title = "Tienda en línea de Pacer | Get More" %}
{% set og_description = "Catálogo de productos | Pasarelas de pagos | Contacto y ubicación | Diseño Web y móvil | Link a Redes Sociales | Búsqueda integrada | Blog integrado" %}
{% set og_img = "https://getmore.mx/img/seo/tienda-en-linea-seo.jpg" %}
{% set og_img_width = "500" %}
{% set og_img_height = "300" %}

{% set view_name = "ourwork" %}
{% set lang_view = "spanish" %}

{% block content %}
<section class="templateb pacer">
  <section class="templateb-cover">
    <div class="return-container">
      <a class="return metropolis-semibold" href="{{tbES.base.linkReturn}}"><img class="semi-triangle" src="{{tbES.base.iconReturn}}" alt="{{tbES.base.iconReturnAlt}}">{{tbES.base.textReturn}}</a>
    </div>
    <div class="templateb-cover-content">
      <img class="templateb-logo" src="{{tbES.pacer.cover.projectLogo}}" alt="{{tbES.pacer.cover.projectLogoAlt}}">
      <h1 class="templateb-cover-title metropolis-bold">{{tbES.pacer.cover.projectName}}&nbsp;<span class="metropolis-medium">|&nbsp;{{tbES.pacer.cover.projectType}}</span></h1>
      <p class="templateb-cover-paragraph metropolis-medium">{{tbES.pacer.cover.projectDesc}}</p>
    </div>
  </section>

  <section class="templateb-data">
    <img class="templateb-data-img" src="{{tbES.pacer.data.laptopImg}}" alt="{{tbES.pacer.data.laptopImgAlt}}">
    <div class="templateb-data-container">
      <div>
        <h2 class="templateb-data-title metropolis-semibold">{{tbES.base.data.title}}</h2>
        <span class="templateb-data-desc metropolis-light"><span class="metropolis-medium">{{tbES.base.data.textYear}}</span>&nbsp;{{tbES.pacer.data.numberYear}}</span>
        <ul class="templateb-data-ul">
          {% for list in tbES.pacer.data.functionalities %}
          <li class="templateb-data-desc metropolis-light">{% if list.title %}<span class="metropolis-medium">{{list.title}}</span>&nbsp;{% endif %}{{list.item}}</li>
          {% endfor %}
        </ul>
        <span class="templateb-data-desc metropolis-light"><span class="metropolis-medium">{{tbES.base.data.locationTitle}}</span>&nbsp;{{tbES.pacer.data.location}}</span>
      </div>
      <div class="templateb-data-btns">
        <a class="btn borderBlack metropolis-bold" href="{{tbES.pacer.data.btnSeeLink}}" target="_blank">{{tbES.base.data.btnSee}}</a>
        <a class="btn metropolis-bold" href="{{tbES.base.data.btnQuoteLink}}">{{tbES.base.data.btnQuote}}</a>
      </div>
    </div>
  </section>
</section>
{% endblock %}
