{% extends "partials/base.njk" %}

{% block title %}Nosotros{% endblock %}

{% set meta_description = "Somos un equipo multidisciplinario. Diseñamos y desarrollamos Tiendas en línea, Páginas Web y Puntos de venta" %}
{% set og_url = "https://getmore.mx/nosotros.html" %}
{% set og_title = "Nosotros | Get More" %}
{% set og_description = "Somos un equipo multidisciplinario. Diseñamos y desarrollamos Tiendas en línea, Páginas Web y Puntos de venta" %}
{% set og_img = "/img/seo/nosotros-seo.jpg" %}
{% set og_img_width = "1848" %}
{% set og_img_height = "708" %}

{% set view_name = "aboutus" %}
{% set lang_view = "spanish" %}

{% block content %}
<section class="about">
  <section class="about-cover">
    <h1 class="about-cover-title metropolis-bold">{{es.aboutus.cover.title}}</h1>
  </section>

  <section class="about-intro">
    <div class="about-intro-content">
      <p class="about-intro-desc metropolis-regular">{{es.aboutus.intro.desc}}&nbsp;<span class="metropolis-bold"><br>{{es.aboutus.intro.descBold}}</span></p>
    </div>
  </section>

  <section class="about-us">
    <div class="about-pillars-title-container">
      <span class="about-bigtext nosotros metropolis-bold">{{es.aboutus.about.bigText}}</span>
      <h2 class="about-pillars-title metropolis-bold">{{es.aboutus.about.title}}</h2>
    </div>
    <p class="about-us-desc metropolis-regular">{{es.aboutus.about.descOne}}<br>{{es.aboutus.about.descTwo}}</p>
    <div class="about-us-card">
      <h3 class="about-us-card-title metropolis-bold">{{es.aboutus.about.card.subtitle}}</h3>
      <div class="about-us-card-desc-container">
        <p class="about-us-card-desc metropolis-regular">{{es.aboutus.about.card.descOnePartOne}}&nbsp;<a class="metropolis-bold" href="{{es.aboutus.about.card.linkOne}}">{{es.aboutus.about.card.descLinkOne}}</a>&nbsp;{{es.aboutus.about.card.descOnePartTwo}}</p>
        <p class="about-us-card-desc metropolis-regular">{{es.aboutus.about.card.descTwoPartOne}}&nbsp;<a class="metropolis-bold" href="{{es.aboutus.about.card.linkTwo}}">{{es.aboutus.about.card.descLinkTwo}}</a>&nbsp;{{es.aboutus.about.card.descTwoPartTwo}}</p>
      </div>
    </div>
  </section>

  <section class="about-pillars">
    <div class="about-pillars-title-container">
      <span class="about-bigtext pilares metropolis-bold">{{es.aboutus.pillars.bigText}}</span>
      <h2 class="about-pillars-title metropolis-bold">{{es.aboutus.pillars.title}}</h2>
    </div>
    <div class="about-pillars-card">
      {% for card in es.aboutus.pillars.card %}
      <div class="about-pillars-content">
        <h3 class="about-pillars-subtitle metropolis-medium">{{card.title}}</h3>
        <p class="about-pillars-desc metropolis-regular">{{card.desc}}</p>
      </div>
      {% endfor %}
    </div>
  </section>

  <section class="about-work">
    <h2 class="about-work-title metropolis-bold">{{es.aboutus.work.title}}</h2>
    <div class="about-work-row">
      <a class="btn metropolis-bold" href="{{es.aboutus.work.btnLink}}">{{es.aboutus.work.btn}}</a>
      <h3 class="about-work-subtitle metropolis-bold">{{es.aboutus.work.bigText}}</h3>
    </div>
  </section>
</section>
{% endblock %}
